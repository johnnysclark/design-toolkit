# macOS
.DS_Store
# Word/Office lock files
~$*
# Node
node_modules/
# Secrets / local env
.env
.env.local
*.local
# Claude local settings
.claude/settings.local.json
# temp
*.tmp
# Precedent Archive — disposable index cache + generated artifacts (rebuilt from disk)
**/.precedent/
TOOLS/precedent-archive/sample-archive/Inbox/
