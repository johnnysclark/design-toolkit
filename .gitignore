# macOS
.DS_Store
# Word/Office lock files
~$*
# Node
node_modules/
# Secrets / local env
.env
.env.local
*.local
# Claude local settings
.claude/settings.local.json
# Local MCP overrides (may contain tokens) — the committed .mcp.json uses env vars only
.claude/.mcp.json
.mcp.local.json
# temp
*.tmp
