import ComingSoon from "@/components/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      title="RAP"
      blurb="The accessibility (RAP) tool — a non-visual / tactile CAD workflow. Also the studio's research showcase."
    />
  );
}
